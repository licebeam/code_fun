const books = [{id: 1, text: 'I am a book', title: 'Salems Lot'}]

const checkOut = () => {
  //take book
}

const checkShelf = () => {
  //look for books
}

const readBook = (book) => {
  //reads a book
}